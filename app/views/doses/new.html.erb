

<div class="container fluid text-center padded feature">
  <div class="row">
    <div class="col-sm-6">
    <%= simple_form_for [@cocktail, @dose] do |f| %>
        <%= f.input :description %>
      <%= f.select :unity, collection: %w(gr. cl. l.) %>
      <%= f.input :ingredient_id, collection: Ingredient.all %>
        <%= f.button :submit %>
      <% end %>
      </div>
  </div>
  </div>
